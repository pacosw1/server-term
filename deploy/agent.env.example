# Bind to this server's Tailscale IPv4 address, never 0.0.0.0.
SERVTERM_LISTEN=100.64.0.10:7843
# Generate independently for each server: openssl rand -hex 32
SERVTERM_AGENT_TOKEN=replace-with-a-random-64-character-token
